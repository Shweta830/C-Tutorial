#include<stdio.h>
int main()
{
    int side,a,b;
    printf("enter the no:");
    scanf("%d%d%d",&side,&a,&b);
    int per;
    per=(side+a+b)*2;
    printf("Rectange of :%d",per);


    
    return 0;
}